{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:cv": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "cache": false
    },
    "build:book-hub": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "cache": false
    },
    "build:notion-api": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "cache": true,
      "env": [
        "TZ",
        "API_SECRET_KEY",
        "SEND_EMAIL_FROM",
        "SEND_EMAIL_TO",
        "SEND_GRID_API_KEY",
        "NOTION_TOKEN",
        "VERCEL_ENV"
      ]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}

{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:book-hub": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "build:notion-api": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "env": [
        "TZ",
        "API_SECRET_KEY",
        "SEND_EMAIL_FROM",
        "SEND_EMAIL_TO",
        "SEND_GRID_API_KEY",
        "NOTION_TOKEN",
        "VERCEL_ENV"
      ]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}

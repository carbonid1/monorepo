import styled from 'styled-components';

const ReviewsWrapper = styled.div`
  display: grid;
  gap: 1rem; // 16px
  grid-auto-rows: max-content;
`;

const $ = {
  ReviewsWrapper,
};

export default $;

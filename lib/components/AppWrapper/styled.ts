import styled from 'styled-components';

const Root = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1 1 0%;
  width: 100%;
  max-width: 64rem;
  padding: 1rem;
  margin: 0 auto;
`;

const $ = {
  Root,
};

export default $;

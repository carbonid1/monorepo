name: Create Notion Journal Entry
on:
  schedule:
    - cron: '0 5 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url 'https://andrew-notion-api.vercel.app/api/create-journal-entry' \
          --header 'Authorization: Bearer ${{ secrets.NOTION_API_SECRET_KEY }}'

#andrew-notion-api.vercel.app
